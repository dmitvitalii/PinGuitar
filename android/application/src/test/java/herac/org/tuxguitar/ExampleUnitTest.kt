package herac.org.tuxguitar

import org.junit.Test

import org.junit.Assert.*

/**
 * This example test file is needed to commit the test dir structure.
 */
class ExampleUnitTest {
    @Test
    fun `addition is correct`() {
        assertEquals(4, 2 + 2)
    }
}
